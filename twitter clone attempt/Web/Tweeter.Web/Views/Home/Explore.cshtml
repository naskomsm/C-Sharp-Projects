@model IEnumerable<TweetListingServiceModel>

@{
    ViewData["Title"] = "Explore";
}

<div class="col-sm-6">
    @foreach (var tweet in Model)
    {
        <div class="card">
            <div class="card-body">
                <p>tweet</p>
            </div>
        </div>
    }
</div>